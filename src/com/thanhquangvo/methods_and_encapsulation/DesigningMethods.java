package com.thanhquangvo.methods_and_encapsulation;

/**
 * Access modifier (no require) like public
 * Optional specifier (no require) like final
 * Return type (require) like void
 * Method name (require) like abc
 * Parameter list (require but can empty) like (int input)
 * Optional exception list (no require) like throws InterruptedException
 * Method body (require but can empty) like { // empty body }
 */
public class DesigningMethods {
}
